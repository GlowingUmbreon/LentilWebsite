{
  "scripts": {
    "serve": "hugo serve --minify --cleanDestinationDir",
    "build": "hugo build --minify --cleanDestinationDir",
    "deploy": "hugo build --minify --cleanDestinationDir && wrangler deploy"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^7.0.2",
    "postcss-cli": "^11.0.1",
    "wrangler": "^4.23.0",
    "lucide-static": "^0.525.0"
  }
}
